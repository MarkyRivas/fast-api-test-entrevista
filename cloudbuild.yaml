steps:
  # Construye la imagen usando tu Dockerfile
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/fast-api-test:$COMMIT_SHA', '.']

# Aquí resolvemos tu error
options:
  logging: CLOUD_LOGGING_ONLY